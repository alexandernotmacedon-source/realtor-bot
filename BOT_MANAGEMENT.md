# Realtor Bot - Управление

## 🚨 Важно: Только один инстанс!

**Никогда не запускайте бота дважды!** Это вызывает ошибку:
```
Conflict: terminated by other getUpdates request
```

## Команды управления

### Проверить статус
```bash
cd /data/.openclaw/workspace/realtor-bot
./monitor-bot.sh
```

### Перезапустить бота (если завис)
```bash
cd /data/.openclaw/workspace/realtor-bot
pkill -f "python.*main.py"
./start-bot.sh
```

### Посмотреть логи
```bash
tail -f /data/.openclaw/workspace/realtor-bot/bot.log
```

### Проверить ошибки
```bash
grep -i "error\|exception\|conflict" /data/.openclaw/workspace/realtor-bot/bot.log | tail -20
```

## Автоматический мониторинг

Бот автоматически проверяется каждые 5 минут. Если упал — перезапускается.

## Частые проблемы

### 1. "Conflict: terminated by other getUpdates request"
**Причина:** Запущено несколько копий бота
**Решение:** 
```bash
pkill -f "python.*main.py"
./start-bot.sh
```

### 2. Бот молчит
**Причина:** Ошибка импорта или падение процесса
**Решение:** Проверить логи и перезапустить

### 3. Ошибки Google Drive (404)
**Причина:** Папки недоступны или удалены
**Решение:** Некритично, бот продолжает работать

## Контакты

При проблемах — пиши мне, разберёмся! 🤖
